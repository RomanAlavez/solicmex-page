---
interface Props {
  name: string;
  description: string;
  index?: number;
}

const { name, description, index = 0 } = Astro.props;

// Cycle through accent shades for visual variety
const accents = [
  "border-cyan-600/50 text-cyan-500",
  "border-cyan-600/50 text-cyan-500",
  "border-cyan-600/50 text-cyan-500",
  "border-white/30 text-white/90",
];
const accentClass = accents[index % accents.length];
---

<div
  class="group relative flex flex-col gap-2 p-5 rounded-xl bg-white/[0.04] border border-white/10 hover:bg-white/[0.07] hover:border-white/20 transition-all duration-300 cursor-default"
>
  <!-- Top accent line -->
  <div
    class={`absolute top-0 left-5 right-5 h-px bg-gradient-to-r ${
      index % 2 === 0
        ? "from-cyan-400/60 to-transparent"
        : "from-red-400/60 to-transparent"
    } opacity-0 group-hover:opacity-100 transition-opacity duration-500`}
  >
  </div>

  <div class="flex items-center gap-3">
    <!-- Numbered index -->
    <span
      class={`text-xs font-black tracking-wider font-mono opacity-40 text-white`}
    >
      {String(index + 1).padStart(2, "0")}
    </span>
    <span class={`text-sm font-bold tracking-wide uppercase border-b pb-0.5 ${accentClass}`}>
      {name}
    </span>
  </div>

  <p class="text-white/60 text-sm leading-relaxed pl-7">
    {description}
  </p>
</div>